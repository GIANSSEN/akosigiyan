# Server-only secret used by /api/chat (api/chat.js on Vercel).
# Get a key at https://aistudio.google.com/apikey
# NEVER prefix with VITE_ - it would be bundled into public client code.
GEMINI_API_KEY=your_gemini_api_key_here
